# PhantomStrike — Configuration File
# Copy this file to config.yaml and fill in your API keys.
# NEVER commit config.yaml to git — it's already in .gitignore.
#
# Required for:
#   Shodan module  → SHODAN_API_KEY
#   GitHub fetch   → GITHUB_TOKEN
#   Metasploit RPC → METASPLOIT section
#   VulnDB         → MYSQL section

SHODAN_API_KEY: ""
GITHUB_TOKEN: ""

METASPLOIT:
  MODULE_PATH: ""
  MSGRPC_PASS: ""

MYSQL:
  USER: ""
  PASS: ""
  HOST: "localhost"
  DB_NAME: "phantomstrike"
